class Controller:
    """
    All controllers will inheret from this so that 'launchPad' can call the starter method
    A 'kill' method will be present in all controllers so that the thread can be terminated if need be
    """

    def starter(self):
        return

    def kill(self):
        return
